// import React, { lazy, Suspense } from 'react';

// const LazyMyWidget = lazy(() => import('./MyWidget'));

// const MyWidget = (props: JSX.IntrinsicAttributes & { children?: React.ReactNode; }) => (
//   <Suspense fallback={null}>
//     <LazyMyWidget {...props} />
//   </Suspense>
// );

// export default MyWidget;
